%li
  %span.content
    = micropost.content
  %span.timestamp
    = "Posted " + time_ago_in_words(micropost.created_at) + " ago"
  - if current_user?(micropost.user)
    = link_to "删除", micropost, method: :delete, data: { confirm: "确认删除?"}, title: micropost.content